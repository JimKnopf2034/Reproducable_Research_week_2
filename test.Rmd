The first R Markdown Test
=========================

```{r}
# To convert this document to html use
# R -e "rmarkdown::render('knitr_example.Rmd')"
```

Here is some text

And some more text, let's load some data stuff

```{r}
library(datasets)
data(airquality)
summary(airquality)
```

Now we make a nice plot:

```{r}
pairs(airquality)
```
